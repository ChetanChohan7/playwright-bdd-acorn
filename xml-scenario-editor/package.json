{
  "name": "xml-scenario-editor",
  "version": "1.0.0",
  "private": true,
  "description": "Local UI to load an XML document, edit its extracted fields, and append the updated XML as a row (Scenario ID, Version, Date, Xml) to a CSV log.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "fast-xml-parser": "^4.4.1"
  }
}
